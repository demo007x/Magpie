## 改动（问题 1：面板描边）

`src/floating/floating.css` 的 `.panel` 规则中移除 `border: 1px solid var(--edge);` 一行——识图/结果面板不再有描边，保留提亮 tint、顶部内高光、原生窗口阴影与圆角。胶囊（`.bar`）和二级菜单（`.chips`）的描边暂不动（问题 2 待定方案后一起调）。

纯 CSS 单行改动，HMR 生效，无需重启。

验证：打开识图结果面板，确认四周无描边、面板整体仍是磨砂白/墨玉黑。确认效果后我们再定问题 2（胶囊底层灰）的处理路线。