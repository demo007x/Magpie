# 拾趣 (Magpie) · 产品路线图

> 本文件描述拾趣 (Magpie) 的发展方向与优先级，内容自包含，可直接打印 / 转 PDF 分发。技术决策的完整推理过程以 ADR（架构决策记录）形式沉淀在仓库的 docs/ 目录中。项目处于早期阶段，按里程碑交付、不承诺具体日期；想影响路线图，欢迎到 GitHub 仓库提 Issue 讨论。

## 当前状态（v0.1.6 · 2026-09-21）

核心体验已完整可用：

- **划词捕获**：拖选 / 双击触发，CGEventTap + AX 双线程，兼容模式兜底（微信 / Office 等无障碍取不到词的应用，自动模拟 ⌘C 并还原剪贴板快照），应用黑名单，防打扰三原则
- **识图捕获**：快捷键 / 托盘框选 → Vision OCR 识别，全部隔离在独立 helper 子进程（崩溃不伤主应用），一步式识图翻译 / 解释 / 总结；钉图（Snipaste 式，可循环回 OCR）
- **动作注册表**：内置 9 个动作（翻译 / 解释 / 总结 AI 流式 + 复制 / 搜索 / 打开链接 / 写邮件 / 复制验证码 / 复制号码），支持上下文智能实体提取（URL / 邮箱 / 验证码 / 电话，单实体一键直达、多实体分组批量操作）；**用户自定义 AI 动作已开放**（名称 + 提示词，与内置动作同池排序，上限 10 个）
- **AI 接入**：BYOK 不锁定，预设 DeepSeek，兼容任意 OpenAI 协议端点；翻译 / 解释 / 总结的 system 提示词开放自定义
- **翻译三通道**（AI / 百度 / DeepL）+ **搜索**（6 内置引擎 + 任意 `{q}` 模板自定义引擎）
- **结果窗**：独立常驻、钉住、拖动、记忆尺寸与位置、底部动作条
- **入口**：托盘菜单、4 个识图全局快捷键（⌥S / ⌥T / ⌥E / ⌥D）、划词、截图
- 更新检测（只提示不自更新）、明暗主题、菜单栏常驻

架构上的两个关键预留：捕获模块 `capture/` 按编译条件分发平台后端（macOS / 其他），两后端遵守同一函数签名，Windows 接入点已就绪；动作注册表是"统一动作模型"的雏形——内置动作与用户自定义动作是同一张注册表的不同类型。

## 近期 — M2「多平台与文字的出口」

优先级从上到下，每项独立交付价值：

1. **划词收藏接入（第一优先）**：把划词内容**带上下文地结构化送达**用户已有的笔记应用——Notion / Obsidian / flomo / Apple 备忘录，候选：语雀 / 飞书文档 / Bear / Craft。发送内容含正文 + 来源应用 + URL + 时间，支持 markdown 模板变量（`{text}` `{source}` `{url}` `{date}`）。接入动作遵循"无状态、一次性送达"原则——不建收藏箱、不做待同步队列、不做中间库，避免与接入对象变成竞争关系。这是产品定位的第一个实物证明："巨头划词是 AI 的终点，拾趣是文字的出口"——用户读到的、识别到的文字，最终流向自己已经建好的知识体系，而不是锁死在对话窗口里。
2. **结果窗追问**：结果窗底部追问框，带原文与上一轮结果多轮对话——预设动作覆盖不了长尾意图，追问把"选动作"变成"说需求"，而结果仍留在可复制、可导出的窗口里。
3. **Windows 版捕获**：实现 UIA 后端，接入已抽象的平台接口——捕获模块按编译条件分发平台后端，两后端遵守同一 `start(tx, debounce_ms)` 签名，Windows 实现照此接入，不在 worker 层加平台分支。
4. **AI 搜索 + 问一问**：多轮浅 agent（tool loop）。依据"热路径零框架"的分层引入策略——消费级划词 90% 场景是浅任务（≤5 次工具调用），框架收益为负（延迟 / 体积 / 调试复杂度），因此 agent 能力此时才按需评估引入（候选：Vercel AI SDK 自定义 transport，复用现有 SSE 哑管道）。
5. **上下文划词**：无障碍查询从"选中文本"扩展到"选区 + 周边段落"，让 AI 动作获得上下文深度理解（事件载荷预留 `context` 字段）。
6. **全局快捷键呼出浮动条**：无需划词，快捷键直接唤起动作面板。
7. **API Key 迁移系统钥匙串**：当前密钥明文存本机配置文件，与主流 BYOK 工具现状一致、设置页已向用户明示，对极客种子用户可接受；迁移到系统钥匙串消除"设备被物理访问可读密钥"的风险，存储模块已预留迁移位。
8. **浮动条贴选区定位**：用无障碍接口读取选区边界、让浮动条贴着选中文本弹出，替代当前的鼠标坐标定位；后者是风险集中度管理的 MVP 选择（省去参数化属性查询的跨应用兼容风险），体验升级列入 M2。

## 中期 — M3「统一动作模型」

内置动作、自定义 AI 动作、接入动作、workflow 步骤收敛为**同一张注册表的不同类型**（设置页搜索引擎的 `{q}` 模板编辑是现成的交互先例）：

- **自定义 workflow**：用户定义多步骤链，AI 步骤与确定性步骤混排（例：划词 → 解释 → 存入 Obsidian）
- **接入动词家族扩展**：在"收藏"之外复制同一接入范式——任务（滴答清单 / Things / Todoist）、学习（Anki / 欧路生词本）、引文（Zotero）
- **方案分享 / 导入**：动作方案一键导出导入，每一份分享都是一次分发（增长引擎）
- **实体识别扩展**：现有 URL / 邮箱 / 验证码 / 电话基于模式与结构规则识别（不用白名单枚举），扩展到诗文名句 / 文献出处 / 表格

## 远期 — M4「免配置」

- **官方托管模型**：无需自备 API Key，服务抽象增加"托管"类型，走账号体系
- **深度研究模式**：Node sidecar（deepagents）按需拉起，注册为 TS 层的一个"重动作"

## 不做清单

产品边界与商业化立场，与功能清单同等重要：

- **不做程序员向功能**（报错解释 / 代码解释），避免与开发者工具赛道正面竞争
- **不自建下游**：不做笔记 / 知识库 / 单词本——用户的沉淀归宿在装拾趣之前已选定，拾趣只缩短到达路径（"入口不改变目的地"）
- **不做广告、不倒流用户数据、不绑自家模型赚 token 差价**：中立枢纽（模型用户自选、数据本地优先、分发通向任意应用）是巨头的商业结构做不到的立场，正是项目可长期持有的护城河，商业化不能背叛它
- **付费切线未定**：先做大用户量，基础功能永久免费 + 模型 BYOK 用户自担；不预建付费基础设施，待用户配置行为数据决定（候选：接入目标数 / 模板能力 / workflow 数量）

## 维护与治理

- **决策可追溯**：重要技术决策以 ADR 记录"为什么是它、否决了什么、翻案条件是什么"（已沉淀 7 条：跨平台框架选 Tauri 2、语言分层 Rust+TS、SSE 哑管道、热路径零框架、无障碍优先 + 兼容模式、浮动条定位、密钥存储），翻案先改 ADR
- **契约文档化**：Rust 与 TS 之间只有三种通信通道（command / event / Channel），接口数刻意保持在个位数，事件与命令协议以契约表为准，改协议必须同步文档
- **变更可追溯**：变更历史遵循 Keep a Changelog 格式 + 语义化版本（SemVer），版本节奏以里程碑为单位
- **贡献流程**：提供贡献指南（Issue 报告要素、本地验证标准：TS 类型检查 + Rust 检查 + 链接验证 + 真机划词、十条工程红线、PR 流程）；文档与代码注释使用简体中文
- **开源协议**：MIT，无附加条款
- **仓库地址**：<https://github.com/demo007x/Magpie>
