# MVP (M1) 执行计划

> v0.1 · 2026-09 · 对应原 01-PRD-MVP（已由 [01-产品定位与规划](./01-产品定位与规划.md) 取代，本文件为 M1 执行的历史记录）；任务粒度以"一次可验证交付"为准

## 里程碑

```
M1-a 工程骨架 + 划词捕获链路（Rust）          ← 本次完成
M1-b 浮动条 + AI 流式动作（TS）               ← 本次完成（代码级，待真机调优）
M1-c 主窗口设置/自检 + 联调打磨               ← 本次完成（代码级）
M1-d 真机验收（PRD §5 DoD）+ 内测发包         ← 待执行（需人工真机测试）
```

## 任务分解

### M1-a 工程骨架与捕获

| #   | 任务              | 交付物                                           | 验收                               | 状态     |
| --- | ----------------- | ------------------------------------------------ | ---------------------------------- | -------- |
| T01 | 仓库与工程骨架    | Cargo/Vite/Tauri 配置、双入口 HTML、capabilities | `cargo check` 与 `pnpm build` 通过 | ✅        |
| T02 | 事件/命令协议落地 | [02] §3.2 全部 command/event 签名                | TS types 与 Rust 定义一致          | ✅        |
| T03 | capture/macos.rs  | CGEventTap + AX 查询 + 状态机 + 过滤             | Debug 构建下各应用拖选/双击发事件  | ✅ 代码级 |
| T04 | 权限检测与自愈    | 权限查询/引导/重试                               | 未授权→引导；授权后 3s 内自愈      | ✅ 代码级 |
| T05 | Windows 占位      | windows.rs stub + trait 对齐                     | win 构建可编译（stub）             | ✅        |

### M1-b 浮动条与 AI

| #   | 任务                | 交付物                                | 验收                 | 状态     |
| --- | ------------------- | ------------------------------------- | -------------------- | -------- |
| T06 | 浮动条窗口管理      | show/定位/防出屏/尺寸调整             | 多屏不出屏，不抢焦点 | ✅ 代码级 |
| T07 | 浮动条 UI           | 胶囊 + 3 动作 + 明暗主题              | 视觉符合 [03] §B.2.3 | ✅ 代码级 |
| T08 | ai.rs SSE 管道      | 流式转发 + error/done 事件 + 60s 超时 | DeepSeek 实测流式    | ✅ 代码级 |
| T09 | 动作注册表 + prompt | [03] §C.3 三模板                      | 输出符合模板要求     | ✅        |
| T10 | 结果面板            | 流式渲染/复制/重试/错误态             | 中断保留已收文本     | ✅ 代码级 |

### M1-c 主窗口与联调

| #   | 任务               | 交付物                            | 验收                 | 状态     |
| --- | ------------------ | --------------------------------- | -------------------- | -------- |
| T11 | 设置页             | Provider CRUD + 默认项 + 动作开关 | 保存后浮动条即时生效 | ✅ 代码级 |
| T12 | 自检页             | 权限状态/跳转系统设置/说明        | 未授权路径完整       | ✅ 代码级 |
| T13 | 黑名单/自身过滤    | 过滤逻辑 + 默认黑名单             | 终端/自身窗口不触发  | ✅ 代码级 |
| T14 | AGENTS.md + README | Agent 指南 + 快速上手             | 新人 30 分钟能跑起来 | ✅        |

### M1-d 真机验收（待人工执行）

| #   | 任务                                                                                         | 说明                                                            |
| --- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| T15 | 验收全清单走查（原 PRD §5 DoD，旧版已作废，以 [01 §5](./01-产品定位与规划.md) 能力快照为准） | Safari/Chrome/备忘录/VS Code/微信；多显示器 DPI；剪贴板不污染   |
| T16 | 性能实测                                                                                     | 松手→弹条 ≤300ms；首字 ≤2s；常驻内存 <120MB                     |
| T17 | 打包分发                                                                                     | `pnpm tauri build`（补 icon：`pnpm tauri icon`）+ 签名/公证调研 |

## 已识别的后续风险清单（M1-d 前必须处理）

1. **AX 兼容性长尾**：DoD 五应用之外的应用逐个实测，记录覆盖矩阵进 [03] §A.6
2. **浮动条不抢焦点的点击兼容性**：macOS 对非激活面板的 hover/click 表现需真机确认；若异常，方案 B = NSPanel（自定义 window subclass）
3. **流式中断恢复**：弱网下 SSE 断流的用户感知处理（MVP=错误态+重试，已可接受）
4. **图标与签名**：上架/分发前必须补（无签名会被 Gatekeeper 拦，且辅助功能权限对无签名应用更敏感）

## M2 预告（下一迭代）

Windows UIA 捕获 → AI 搜索 + 问一问（AI SDK + tool loop）→ 上下文划词 → 托盘/快捷键 → Key 迁 keyring → 结果面板 Markdown 渲染
